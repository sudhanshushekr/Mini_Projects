[tool.poetry]
name = "flask-restapi"
version = "0.1.0"
description = ""
authors = ["sudhanshu shekahr"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
flask = "^3.0.3"
flask-smorest = "^0.44.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
